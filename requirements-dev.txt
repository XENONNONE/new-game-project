# Development and testing dependencies for GestureLSM Avatar Runtime
# Install with: pip install -r requirements-dev.txt

# Core runtime dependencies (mirrors GestureLSM/requirements.txt for the runtime subset)
torch==2.6.0
numpy==1.26.2
omegaconf
PyYAML
soundfile
librosa

# Inference runtime extras
kokoro==0.9.4
misaki[en]

# Development tools
pytest>=8.0
pytest-cov>=4.0
ruff>=0.6.0
mypy>=1.10
pytest-xdist>=3.5  # parallel test execution
